{
  "name": "lab5-query-builder",
  "version": "1.0.0",
  "description": "Система типизированных запросов",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "typescript": "^5.0.0"
  }
}